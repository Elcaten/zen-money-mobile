import * as React from 'react';
import {Text} from '../../components';

export interface EditAccountScreenProps {}

export const EditAccountScreen: React.FC<EditAccountScreenProps> = (props) => {
  return <Text>EditAccountScreen</Text>;
};
