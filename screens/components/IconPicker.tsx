import * as React from 'react';
import {Text} from '../../components';

export interface IconPickerProps {}

export const IconPicker: React.FC<IconPickerProps> = (props) => {
  return <Text>IconPicker</Text>;
};
